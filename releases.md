# Releases

Release notes for the Clinical-AI-Demos repository.

---

3-Robot Camarade Swarm Instructions for Demo Prompt 07 (v0.3.0)
v0.3.0 - Multi-Robot Synergy Code Generation Instructions for the 24/7 Adverse Event Response Demo

## Summary

- Delivered a comprehensive directory tree of code generation instructions at `demo-projects/07-humanoid/paper/instructions/` extending demo prompt 07 (Humanoid 24/7 Adverse Event Response Team) with multi-robot synergy semantics. A future Claude Code Opus 4.7 1M Max session reads this tree alongside the existing `demo-projects/07-humanoid-24-7-adverse-event-response.md` prompt to author a complete 168-hour 4-site camarade swarm simulation across seven sequential commits in a single pull request.
- The v0.3.0 release captures five multi-robot synergy modifications to the original prompt 07: swarm behavior between 3 H2 humanoids per site (not 3 rotating across 4 sites; 12 total in v0.3.0), physical communication via 60 GHz ultra-wideband peer mesh plus IR beacon line-of-sight (5 ms UWB round trip plus 1 ms IR beacon round trip), intellectual communication via the shared on-premises Claude Code compute fabric (per-site Claude Opus 4.7 1M instance plus central read-only observer bus), one broadcast tick sent simultaneously to all 3 robots per site at 1 Hz LLM cadence (3 sub-commands per broadcast for the 3 named roles Lead, Assist, Reserve), and peer-aware adaptation that treats patients, doctors, and other robots as first-class actors at every tick.
- Encoded the v0.3.0 thesis statement throughout: On-premises repository based LLMs provide commands to humanoid robots based on real-time sensor data and controlled via x, y, z coordinates to administer synergistic treatment to patients adverse events. This workflow minimizes single robot error potential. The camarade pattern reduces single-robot error potential by a factor of approximately 3 through peer cross-checking of sensors, role rotation on fault, hand-off within 2 seconds, and swarm-wide E-stop within 5 ms.
- Authored the LLM working memory notes documenting how a future Claude Code session processes the 168-hour monitoring window (604,800 s, 604,800 ticks at 1 Hz LLM cadence, 6,048,000 ticks at 10 Hz humanoid motion cadence per H2 when active) without context window truncation, especially for later time commits. Notes include the three-layer chunking strategy (L0 raw archived to Zenodo, L1 minute Parquet, L2 hour JSONL, L3 day Markdown) and per-commit size budgets that keep each commit at most 15 new files and 100 KB of new content.
- The seven commit roadmap distributes the instruction generation work across the 1M context window efficiently in one PR. The 2nd to last commit is designated for fixing all errors and authoring the pytest suite plus the 7-check error scan script. The last commit is dedicated to repository-level updates (top README, releases.md prepended entry, CHANGELOG.md v0.3.0 block, demo-projects README update, BibTeX entry pointing to the author's prior FAERS LLM work at DOI 10.5281/zenodo.18029100).
- Excluded the extra-hours dataset from `physical-ai-oncology-trials/new-trial/national-24-7-trial/extra-hours/` from the future code generation inputs per the v0.3.0 brief. Only `hour-00/` through `hour-55/` are read by the future session.
- Single dashes only throughout the instruction tree. Black text only. ASCII diagrams cap at 80 columns by 60 lines. All patient identifiers are synthetic of the form `PAT-NET-001-PNNN`. No real PHI.
- The v0.3.0 release adds no Python and no YAML source files that would trigger CI lint failures. The instruction tree is Markdown only at the time of this PR; the future code generation session will populate `src/`, `config/`, `schemas/`, `data/`, `diagrams/`, `notebooks/`, `reports/`, and `figures/` subdirectories within `demo-projects/07-humanoid/paper/instructions/` per the per-commit roadmap. The existing `ruff.toml` per-file-ignores entry `"demo-projects/**/*.py" = ["F401", "F402", "F821"]` covers the future source. The 3 failing checks pattern (Cl / lint-and-format on Python 3.10, 3.11, 3.12) called out in the project brief is prevented by the markdown-only scope of this PR plus the documented pre-commit checklist for the future session.

## Features

- `demo-projects/07-humanoid/paper/instructions/README.md` - Comprehensive README with 9 badges (Demo, Release, Companion, DOI, Prior DOI for 10.5281/zenodo.18029100, Humanoid 3x Unitree H2 per site, LLM Claude Opus 4.7 1M on-prem, Python 3.10/3.11/3.12, MIT License), the v0.3.0 thesis statement, an inventory at a glance table with 14 properties, the directory structure, a high-level ASCII diagram of the 4-site continental network with the central on-prem Claude Code compute bus, a Mermaid diagram for the single-site swarm, the future output footprint, and a BibTeX block citing the author's prior FAERS LLM paper.
- `demo-projects/07-humanoid/paper/instructions/00-project-overview/` - 6 foundation documents: architecture.md (6 layers from site to cross-site bus, decision cadence summary, failure modes, companion repo inputs minus extra-hours), swarm-overview.md (the 3 H2 camarade pattern definition, the 3 named roles, the 5-component priority score, physical and intellectual communication, the 7 camaraderie invariants), thesis.md (8-section justification of on-prem repository-based LLMs commanding humanoid robots via x, y, z coordinates for synergistic treatment), working-memory-notes.md (the 168-hour window, chunking strategy, commit processing rules, late commit memory pressure mitigations, tick math cheat sheet, commit size budget table), camaraderie.md (concrete camaraderie expression in config, schemas, source code, iteration, comparison, diagrams, figures, reports, and the one-sentence test), network-layout.txt (ASCII visualization of all 12 H2 humanoids across 4 swarms).
- `demo-projects/07-humanoid/paper/instructions/01-commit-roadmap/` - 12 documents: the 7-commit roadmap, the commit 6 error scan script authoring instructions, the 5 pytest module authoring instructions (schemas, swarm invariants, kinematics, xyz_safety, ctcae_grader), the conftest.py instructions, the site_frame_bounds.yaml authoring, the CI compliance note documenting the 3-failing-checks fix pattern, the ctcae_decision_table.csv authoring.
- `demo-projects/07-humanoid/paper/instructions/02-config-instructions/` - 8 documents authoring the 6 YAML config files plus pyproject.toml plus docker-compose.yml: network.yaml (4-site definition with synthetic addresses, 200 enrolled patients, 12 H2 inventory, cross-site bus with phi_egress forbidden), h2_humanoid.yaml (12 robots tagged by camarade_group, shared specs, response kit, starting roles), site_coordination.yaml (1 Hz broadcast with 3 fan-out targets and 3 acks, degraded modes for LLM down, UWB down, IR beacon down, cross-site summary with 9 de-identified fields, ed25519 signing per site), swarm_coordination.yaml (4 named camarade groups, 3 named roles, 5-component priority weights, Hungarian bipartite match rotation, comms_physical UWB plus IR beacon, comms_intellectual pub-sub, 7 camaraderie invariants, camarade aggressiveness sweep axis, 1-2-3 robot down fallback), llm_loop.yaml (Claude Opus 4.7 1M on-prem at 1 Hz with 200 ms latency budget, 3 sub-command broadcast, world model fields, cross-site observation read-only at 1 hour cadence, no-LLM fallback), escalation_rules.yaml (CTCAE grade 3+ physician escalation, FDA RTCT 1 hour SLA, sponsor acknowledgment 1 hour SLA, swarm fault escalation for 1/2/3 robots down, perimeter breach, battery low at 25 percent), pyproject.toml (Python 3.10-3.12 with ruff cascade, 11 runtime dependencies including anthropic and cryptography, dev dependencies for ruff yamllint pytest), docker-compose.yml (4 per-site LLM services, 1 ingest, 1 simulator, 1 db, 1 cross-site-bus with phi_egress forbidden).
- `demo-projects/07-humanoid/paper/instructions/03-schemas-instructions/` - 12 documents authoring the 10 JSON Schemas plus samples.jsonl plus ingest.py: humanoid_command (with peer_robot_ids array of exactly 2 entries enforcing the camarade pattern, audit chain prev hash, synergy score), swarm_message (new, with 10 payload kinds including handoff_request handoff_ack sensor_share e_stop_propagation), ae_event (with responding_swarm array of exactly 3 robot IDs), ctcae_grading (with grader enum including swarm_consensus for majority vote across 3 robots), sponsor_acknowledgment (HR 9505 1 hour SLA), fda_rtct_submission (1 hour SLA with hash chain and ed25519 signature), physician_escalation (CTCAE grade 3+ pager plus phone with 60 second ack), llm_decision (per-tick broadcast with exactly 3 sub_commands one per role, peer_observability flags), robot_camarade_state (new, with self_state plus peer_state_a plus peer_state_b published at 1 Hz), peer_handoff (new, with 2 second SLA per hand-off), the representative samples.jsonl validation, and the jsonschema-based ingest.py validator.
- `demo-projects/07-humanoid/paper/instructions/04-robotic-instructions/` - 8 documents authoring the robotic source files: h2_dispatcher.py (fans out 3 sub-commands as one atomic broadcast, tracks 3 acks within 500 ms, advances audit chain), swarm_coordinator.py (Hungarian bipartite match for role assignment, peer-aware priority, 7 camaraderie invariants checker, synergy_score for 3-2-1 active robots), robot_loop.cpp (10 Hz motion loop with 5 ms E-stop reaction, 5-deep circular command buffer, cross-OS build under clang++ g++ MSVC, 39 joint controller), comms_physical.py (60 GHz UWB at 200 Hz with 5 ms round trip and 0.1 percent drop rate, IR beacon at 1000 Hz with 1 ms round trip), comms_intellectual.py (1 Hz publish-subscribe over on-prem Claude Code fabric, peer state cache with 1.1 second staleness), peer_state_tracker.py (self plus 2 peer state model with dead reckoning when peer publish is stale), ae_response_flow.txt (3-lane swimlane Lead Assist Reserve over 90 seconds), swarm_dance.txt (new ASCII top-down view showing 3 robots orbiting the patient bed over 10 seconds), Cargo.toml (Rust runner crate manifest with crossbeam, parquet, arrow, serde, rayon).
- `demo-projects/07-humanoid/paper/instructions/05-cartesian-instructions/` - 4 documents authoring the cartesian source files: kinematics.yaml (Unitree H2 full 39 DOF chain with spine, arms, legs, joint ranges, DLS solver settings), sensor_to_xyz.py (RGB-D camera depth-to-world projection, IR beacon plus UWB Kalman fusion for peer triangulation, shared site frame with origin at site entrance), cartesian_planner.py (quintic polynomial trajectory blending, 0.4 m hand-off and 1.2 m rest distance enforcement, envelope check against peer poses), xyz_safety.py (frame bounds plus patient envelope plus no-fly zone validation, 2 second peer envelope prediction), and the sample_sensor.csv plus sample_xyz.csv 60-second illustrative streams.
- `demo-projects/07-humanoid/paper/instructions/06-iteration-instructions/` - 6 documents authoring the iteration runtime: iterations.yaml (32 iterations with Latin Hypercube Sampling across 5 axes seed/llm_temperature/ae_arrival_jitter/response_time_variance/camarade_aggressiveness, partitioned by site and day, L0/L1/L2/L3 layering), week_runner.py (concurrent.futures ProcessPoolExecutor with 4 site workers for the 168-hour window), iterate.py (32-iteration sweep with DuckDB aggregate at the end), runner.rs (Rust crossbeam alternative for production-scale sweeps), per_day_parquet.md (zstd level 3 compression with site plus day partitioning), aggregate_duckdb.md (7 tables across iterations, per_site, per_swarm, per_day, per_ae_type, camaraderie_invariants, handoffs), run_log_notebook.md (Jupyter notebook with 8 cells), run_log_record_layout.md (index.jsonl schema with 14 fields per iteration).
- `demo-projects/07-humanoid/paper/instructions/07-llm-planner-instructions/` - 7 documents authoring the LLM control loop: llm_planner.py (per-tick 1 Hz Claude Opus 4.7 1M call returning a 3-sub-command llm_decision, prompt built from world model, response validated against JSON Schema), broadcaster.py (atomic publish to site/{site_id}/broadcast topic with 3 ack tracking and audit chain advancement), shared_compute.md (architecture document for the central on-prem Claude Code compute server as passive observer that never writes to robots), ctcae_grader.py (CTCAE v5.0 decision-table-driven grading per robot plus swarm consensus majority vote with physician escalation on grade disagreement of 2 or more), fda_rtct_submitter.py (1 hour SLA enforced, ed25519 signed payload, hash chain), physician_escalation.py (CTCAE grade 3+ pager plus phone with 60 second ack, IV access blocked until physician confirmation), architecture_summary.md (single-page module role table at 1 Hz, 10 Hz, 200 Hz, 1000 Hz cadences plus the glue site_runtime.py wiring).
- `demo-projects/07-humanoid/paper/instructions/08-comparison-instructions/` - 10 documents authoring the comparison framework: comparison_methodology.md (3 baseline categories prior versions plus competitor humanoid configurations plus hybrid configurations, 7 dimension weights summing to 1.00 with new Camaraderie dimension at 0.10), metrics.schema.json (26 keys across 8 Response Time, 6 Patient Safety, 4 FDA RTCT, 4 Camaraderie, 4 other), human_team_baseline.csv (30 rows from 6 published trial site studies as the 3-human-paramedic team reference), compute.py (per-iteration plus per-configuration plus ranking calculation), compare_agent.py (LLM agent authoring the report narrative), prompt_frozen.md (the locked system prompts for planner and comparison agent), report.md plus report.pdf (canonical comparison report with dedicated Camaraderie section as the v0.3.0 differentiator), dashboard.html (single-page static HTML with 7 tabs), 6 figures at 300 dpi white facecolor matplotlib (01_swarm_architecture, 02_response_time_histogram, 03_camaraderie_heatmap which is the v0.3.0 new figure, 04_role_rotation_timeline, 05_force_budget_distribution, 06_4site_comparison), comparison.json (machine-readable output for the dashboard), h2_swarm_outcomes.parquet (per-AE outcome record across all 32 iterations with 21 columns).
- `demo-projects/07-humanoid/paper/instructions/09-runtime-instructions/` - 3 documents covering MacOS, Windows, and Linux runtime: MacOS section (Mac Studio M2 Ultra, brew install python rust duckdb cmake, full 168-hour run in 12 minutes, 32-iteration sweep in 6.5 hours), Windows section (HP Z8 G5, Lenovo ThinkStation P8 with 96 cores at 5 hours sweep wall clock, PowerShell venv plus pip install, Visual Studio 2022 C++ for the robot loop, CMake), Linux section (Dell PowerEdge R760, Supermicro AS-2025HS-TNR with 192 cores at 4 hours sweep wall clock, Ubuntu 22.04/24.04, apt python rust cargo, docker-compose).
- `demo-projects/07-humanoid/paper/instructions/01-commit-roadmap/commit-6-error-scan.md` plus `check_errors_script.md` plus `ci_compliance_note.md` - The 7-check error scan covers schema field coverage, YAML config validation, cartesian frame bounds, robot ID coverage, ASCII diagram size (80x60 cap), dash style (no em, no double, no triple in prose), and PHI absence. The CI compliance note documents the historical 3-failing-checks pattern at Cl / lint-and-format on Python 3.10, 3.11, 3.12 and the pre-commit checklist that prevents it.
- The instructions tell the future Claude Code Opus 4.7 1M Max session to read 17 specific files from `kevinkawchak/physical-ai-oncology-trials` (README, FDA-April-2026, Background-A and Background-B chunk sets, hour-00 through hour-55 verbatim only excluding extra-hours, hr_9504_error_reduction.tex and hr_9505_realtime_sponsor.tex, patient-journey/stage_09_surveillance.py, agentic-ai examples, regulatory/ich-gcp plus fda-compliance, regulatory-submit audit_trail.py, privacy/breach-response, unification usl humanoids, competitions instructions chunking_strategy.md plus competition_protocol.md). The extra-hours dataset (hour-56 through hour-83) is explicitly excluded from v0.3.0 inputs.
- The instructions tell the future session to author the 12 H2 humanoids (3 per site, 4 sites = 12 total) where each robot has a stable identity within the site naming space (SF-01-H2-A through SF-01-H2-C at San Francisco, SD-01-H2-D through SD-01-H2-F at San Diego, BO-01-H2-G through BO-01-H2-I at Boston, AT-01-H2-J through AT-01-H2-L at Atlanta). Each robot has a starting role at simulation t=0 (Lead, Assist, or Reserve) that rotates at every 1 Hz tick based on the Hungarian bipartite match against the 5-component priority score.
- CHANGELOG.md adds the `[0.3.0] - 2026-05-17` block above the `[0.2.0]` entry. Captures all the instruction tree additions in `demo-projects/07-humanoid/paper/instructions/` plus the Changed entries (top-level README.md v0.3.0 block plus repository structure tree plus Mermaid diagrams plus humanoid coverage diagram plus Citation section addition of the prior FAERS DOI; demo-projects/README.md v0.3.0 row addition plus 07-humanoid instructions section) and the Notes block (CI compliance, markdown-only scope, character rules, release placement, prior DOI reference).
- releases.md adds the v0.3.0 release entry at the top of the file in the Summary / Features / Contributors / Notes format. The v0.2.0 and v0.1.0 entries remain immediately below in chronological order.
- README.md (top-level) adds the v0.3.0 release block above the v0.2.0 release block at the top of the README, updates the Release badge to v0.3.0, adds the Prior DOI badge for 10.5281/zenodo.18029100, adds the `demo-projects/07-humanoid/paper/instructions/` subtree to the repository structure tree, adds the v0.3.0 box to the demo flow ASCII diagram, updates the Mermaid repository topology diagram with the SW node connected to the companion repo NTR with an excludes extra-hours edge, updates the Mermaid demo prompt execution loop with the SW input box, updates the humanoid coverage section to clarify 3 per site for prompt 07 in v0.3.0, updates the Citation section to include the prior FAERS LLM BibTeX entry.
- demo-projects/README.md updates the Release badge to v0.3.0, adds a Swarm Instructions for Prompt 07 (v0.3.0) section that lists the directory tree and the 7-commit roadmap, updates the Coverage Matrix and Humanoid Coverage diagram with 3 H2 per site for prompt 07.
- CITATION.cff updates the version to 0.3.0 and the date-released to 2026-05-17.

## Contributors
@kevinkawchak
@claude

## Notes

The v0.3.0 release does not produce simulation files. The release delivers a comprehensive set of code generation instructions at `demo-projects/07-humanoid/paper/instructions/` that a future Claude Code Opus 4.7 1M Max session reads to author the 3-robot camarade swarm 24/7 AE response simulation across seven sequential commits in a single pull request. The instructions are intentionally specific: every config field, every JSON Schema property, every Python module interface, every Rust crate dependency, every C++ class skeleton, every YAML key is named. The future session executes the instructions deterministically with minimal interpretive work. Strong multi-robot synergy is the binding design property: 3 H2 humanoids per site, one broadcast per tick to all 3, peer 60 GHz UWB plus IR beacon physical channel, shared on-prem Claude Code compute fabric intellectual channel, peer-aware adaptation to patients doctors and other robots, and the 7 camaraderie invariants. The thesis is: On-premises repository based LLMs provide commands to humanoid robots based on real-time sensor data and controlled via x, y, z coordinates to administer synergistic treatment to patients adverse events. This workflow minimizes single robot error potential. The v0.3.0 release excludes the extra-hours dataset (hour-56 through hour-83 of the companion repo) and reads only hour-00 through hour-55 verbatim. The 168-hour monitoring window is 604,800 seconds at 1 Hz LLM cadence (604,800 broadcast ticks per site) and 10 Hz humanoid motion cadence (6,048,000 motion ticks per active H2 per week). With 3 H2 per site and 4 sites, the upper bound is 2,419,200 LLM broadcasts and 72,576,000 motion ticks per week network-wide; the three-layer chunking strategy (L0 raw archived to Zenodo, L1 minute Parquet, L2 hour JSONL, L3 day Markdown) keeps the future LLM context window safe across all 7 commits with no late-commit truncation. The 2nd to last commit is dedicated to fixing all errors found in the prior 5 commits and authoring the pytest suite plus the 7-check error scan script. The last commit handles repository-level updates including the prepended releases.md entry, the CHANGELOG.md v0.3.0 block, the top README.md release block, the demo-projects README.md update, and the CITATION.cff version bump. All commits in the future PR are autonomous; the present v0.3.0 PR is also authored autonomously in 7 sequential commits without user interaction. The release adds no Python and no YAML source files so the CI lint-and-format workflow on Python 3.10, 3.11, 3.12 remains green; the existing `ruff.toml` per-file-ignores entry `"demo-projects/**/*.py" = ["F401", "F402", "F821"]` covers the future source files that the downstream session will author. The release also cites the author's prior FAERS LLM paper at DOI 10.5281/zenodo.18029100 as the precedent for LLM-driven adverse event work; the BibTeX block is included in the top-level README.md Citation section and in the v0.3.0 instructions README.md.

---

100 Image Instructions for the 10 Demo Prompts (v0.2.0)
v0.2.0 - Humanoid + LLM Demo Image Instruction Set for All 10 Prompts

## Summary

- Delivered 100 publication-ready image instruction Markdown files at `demo-projects/image-instruct/`, 10 per prompt across 10 prompt-specific subdirectories. Each instruction is a self-contained specification that a future Claude Code Opus 4.7 1M Max session reads to author one professionally coded matplotlib Python script and emit one 300 dpi PNG output under `demo-projects/NN-name-output/figures/NN-NN-short-name.png`. The 100 instructions span the same fixed set of 10 chart types per prompt: 3 landscape full-page (system architecture and humanoid + LLM control loop, operational timeline Gantt swimlane, regulatory and risk compliance heatmap matrix) plus 7 portrait (value proposition canvas, financial assessment waterfall, capability radar comparison, sankey flow diagram, process funnel chart, strategic quadrant bubble matrix, decision tree flowchart). 30 landscape and 70 portrait at Letter size (11.0 x 8.5 in landscape, 8.5 x 11.0 in portrait) totaling 100 instructions.
- Each instruction file centers a specific humanoid platform and a specific LLM control loop across the 10 prompts.

## Features

See the v0.2.0 detailed entry in commits prior to v0.3.0 for the full Features block. Summary: 100 image instructions in 10 subdirectories, shared 10-color palette, DejaVu Sans typography, matplotlib-only Python conventions, and the README at `demo-projects/image-instruct/README.md`.

## Contributors
@kevinkawchak
@claude

## Notes

The v0.2.0 release delivers 100 image instructions that future Claude Code Opus 4.7 1M Max sessions read to author 100 publication-ready matplotlib figures at 300 dpi for the 10 demo prompts in this repository.

---

Humanoid + LLM Oncology Trial Demo Prompts (v0.1.0)
v0.1.0 - Humanoid and LLM Demo Prompts for Physical AI Oncology Clinical Trials

## Summary

- Established the Clinical-AI-Demos repository as a companion to kevinkawchak/physical-ai-oncology-trials. The v0.1.0 release delivers 10 standalone Claude Code task brief prompts at `demo-projects/` that future Claude Code Opus 4.7 1M Max sessions read to author Physical AI oncology clinical trial demonstrations.

## Features

See the v0.1.0 detailed entry for the 10 demo prompts, the demo-projects README, the repository scaffolding (CI workflow, ruff and yamllint configuration, governance files), and the national-repositories meta-prompting guide.

## Contributors
@kevinkawchak
@claude

## Notes

The v0.1.0 release delivers 10 self-contained Claude Code task brief prompts that future Claude Code Opus 4.7 1M Max sessions read to author the simulations across seven sequential commits in a single pull request per prompt.
